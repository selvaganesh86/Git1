import pandas as pd
import numpy as np
from scipy inmport stats
from pyod.models.knn import KNN

def scm(bu_var, word_var, te_amt_var, scm_org_data, sheet_id)
  # create new filtered data frame based on parameters received
  
